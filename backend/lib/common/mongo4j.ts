const mongo4j = require('mongo4j');
import config from '../config';

export default {
    // init: () => {
    //     mongo4j.init(`${config.N4J_PROTOCOL}://${config.N4J_HOST}`, {user: config.N4J_USER, pass: config.N4J_PASS});
    // }
};